import {
  userProfile
} from './userProfile';

import {
  tokenData
} from './tokenData';

import {
  messages
} from './messages';

import {
  sharedMessages
} from './sharedMessages';

import {
  myMessages
} from './myMessages';

import {
  deleteMessage
} from './deleteMessage';

import {
  editMessage
} from './editMessage';

import {
  updateMessage
} from './updateMessage';

export default [
  userProfile,
  tokenData,
  messages,
  sharedMessages,
  myMessages,
  deleteMessage,
  editMessage,
  updateMessage
];
